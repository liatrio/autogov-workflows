name: Build Entrypoint Temp Test

on:
  workflow_dispatch:
  push:
    branches: ['**']
    paths-ignore:
      - .github/workflows/wf-slack-alert.yaml
      - README.md
      - CONTRIBUTING.md
      - catalog-info.yaml
      - renovate.json
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [main]

permissions: {}

jobs:
  attest-image-hp: #image
    if: github.event.pull_request.draft == false
    permissions:
      id-token: write
      attestations: write
      packages: write
      contents: write
      actions: read
    uses: ./.github/workflows/rw-hp-build-image-temptest.yaml
    secrets: inherit
    with:
      subject-name: ${{ github.repository }}
      registry: ghcr.io
      cert-identity: https://github.com/liatrio/liatrio-gh-autogov-workflows/.github/workflows/rw-hp-attest-image-temptest.yaml@${{ github.ref }}

  # attest-blob-hp: #blob
  #   if: github.event.pull_request.draft == false
  #   permissions:
  #     id-token: write
  #     attestations: write
  #     packages: read
  #     contents: write
  #     actions: read
  #   uses: ./.github/workflows/rw-hp-build-blob.yaml
  #   secrets: inherit
  #   with:
  #     subject-path: |
  #       i_am_blob
  #       i_am_another_blob
  #     cert-identity: https://github.com/liatrio/liatrio-gh-autogov-workflows/.github/workflows/rw-hp-attest-blob.yaml@${{ github.ref }}

  # attest-blob-lp: #blob
  #   if: github.event.pull_request.draft == false
  #   permissions:
  #     id-token: write
  #     attestations: write
  #     packages: read
  #     contents: write
  #     actions: read
  #   uses: ./.github/workflows/rw-lp-build-blob.yaml
  #   secrets: inherit
  #   with:
  #     subject-path: |
  #       i_am_blob
  #       i_am_another_blob
  #     cert-identity: https://github.com/liatrio/liatrio-gh-autogov-workflows/.github/workflows/rw-lp-attest-blob.yaml@${{ github.ref }}
