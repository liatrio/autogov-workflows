name: Attest Blob Low Permissions Reusable Workflow

on:
  workflow_call:
    inputs:
      subject-path:
        description: >
          Path to the artifact serving as the subject of the attestation. Must
          specify exactly one of "subject-path" or "subject-digest". May contain a
          glob pattern or list of paths (total subject count cannot exceed 2500).
          examples: https://github.com/actions/attest-build-provenance?tab=readme-ov-file#examples
        required: true
        type: string
      blob-artifact-name:
        description: >
          The name of artifact for the blob(s) built from the build-blob action found in
          ./.github/actions/build-blob/action.yaml.
        default: blob-build-artifact-low-perms
        required: false
        type: string
      show-summary:
        description: >
          Whether to attach a list of generated attestations to the workflow run
          summary page. Defaults to true.
        default: true
        required: false
        type: boolean
      workflow-runner-label:
        description: >
          The label used for runner/OS selection
          examples: https://docs.github.com/en/actions/writing-workflows/choosing-where-your-workflow-runs/choosing-the-runner-for-a-job#example-specifying-an-operating-system.
        required: false
        default: ubuntu-latest
        type: string
      github-token:
        description: >
          The GitHub token set throughout the reusable workflow including the composite (build) action.
        required: false
        type: string
      autogov-helper-version:
        description: The autogov-helper version to use.
        required: false
        type: string
        default: v0.4.2
    outputs:
      blob-artifact-id:
        description: The artifact-id of the blob artifact(s).
        value: ${{ jobs.build-low-perms.outputs.blob-artifact-id }}
      attest-build-attestation-artifact-id:
        description: The artifact-id of the build provenance attestation artifact.
        value: ${{ jobs.attest-build-low-perms.outputs.provenance-artifact-id }}
      attest-metadata-attestation-artifact-id:
        description: The artifact-id of the custom metadata attestation artifact.
        value: ${{ jobs.attest-metadata-low-perms.outputs.metadata-artifact-id }}
      attest-sbom-attestation-artifact-id:
        description: The artifact-id of the SBOM attestation artifact.
        value: ${{ jobs.attest-sbom-low-perms.outputs.sbom-artifact-id }}
      attest-dependency-scan-attestation-artifact-id:
        description: The artifact-id of the dependency scan attestation artifact.
        value: ${{ jobs.attest-dependency-scan-low-perms.outputs.dependency-scan-artifact-id }}

permissions: {}

env:
  GH_TOKEN: ${{ inputs.github-token || github.token }}
  ARTIFACTS_FOLDER: ./artifacts
  ATTESTATIONS_FOLDER: ./attestations
  POLICY_REF: https://github.com/liatrio/liatrio-rego-policy-library
  OTEL_EXPORTER_OTLP_ENDPOINT: o11y-gh-traces-grpc-receiver.dev.k8s-platform-v3.liatr.io:443
  OTEL_EXPORTER_OTLP_PROTOCOL: grpc
  OTEL_EXPORTER_OTLP_HEADERS: ""
  OTEL_SERVICE_NAME: ${{ github.event.repository.name }}

jobs:
  build-low-perms:
    permissions:
      contents: read
    runs-on: ${{ inputs.workflow-runner-label }}
    outputs:
      blob-artifact-id: ${{ steps.upload-blob.outputs.artifact-id }}
    steps:
      - name: Fail if Runner is self-hosted
        if: ${{ runner.environment != 'github-hosted' }}
        run: |
          echo "Job is running on a self-hosted runner. Terminating job..."
          exit 1
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Build Blob
        if: ${{ runner.environment == 'github-hosted' }}
        id: build-blob
        uses: ./.github/actions/build-blob
      - name: Upload Blob
        id: upload-blob
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        with:
          name: ${{ inputs.blob-artifact-name }}
          path: ${{ inputs.subject-path }}
          if-no-files-found: error

  attest-build-low-perms:
    runs-on: ${{ inputs.workflow-runner-label }}
    needs: build-low-perms
    permissions:
      id-token: write
      attestations: write
      contents: read
      actions: read
    env:
      ARTIFACT_ID: ${{ needs.build-low-perms.outputs.blob-artifact-id }}
    outputs:
      provenance-artifact-id: ${{ steps.upload-provenance.outputs.artifact-id }}
    steps:
      - name: Fail if Runner is self-hosted
        if: ${{ runner.environment != 'github-hosted' }}
        run: |
          echo "Job is running on a self-hosted runner. Terminating job..."
          exit 1
      - name: Download Blob Artifact(s)
        if: ${{ runner.environment == 'github-hosted' }}
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6.0.0
        with:
          github-token: ${{ inputs.github-token || github.token }}
          artifact-ids: ${{ env.ARTIFACT_ID }}
          path: ${{ env.ARTIFACTS_FOLDER }}
          merge-multiple: true
      - uses: octo-sts/action@e480437973a6f6ac2e9caa40ecabedc870d76395 # v1.0.1
        id: octo-sts
        with:
          scope: liatrio
          identity: autogov-infra
      - name: Install autogov-helper
        run: |
          set +x
          export GH_TOKEN=${{ steps.octo-sts.outputs.token }}
          gh release download ${{ inputs.autogov-helper-version }} \
            --repo liatrio/autogov-helper \
            --pattern "autogov-helper"
          chmod +x autogov-helper
      - name: Attest Provenance
        id: attest-build-provenance
        uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3.0.0
        with:
          subject-path: ${{ env.ARTIFACTS_FOLDER }}
          github-token: ${{ inputs.github-token || github.token }}
          push-to-registry: false
      - name: Rename Provenance Bundle
        run: |
          mkdir ${{ env.ATTESTATIONS_FOLDER }}
          mv ${{ steps.attest-build-provenance.outputs.bundle-path }} ${{ env.ATTESTATIONS_FOLDER }}/attest-build-provenance-attestation.jsonl
      - name: Emit Provenance Attestation Content
        uses: liatrio/run-with-telemetry@17cf80578d78573a8895cdbc9a4c2a1fbb707788 # latest
        with:
          otel-exporter-otlp-endpoint: ${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
          otel-service-name: ${{ env.OTEL_SERVICE_NAME }}
          github-token: ${{ inputs.github-token || github.token }}
          step-name: Emit Provenance Attestation Content
          run: |
            set +x
            export OTEL_EXPORTER_OTLP_ENDPOINT=https://${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
            ./autogov-helper att-span --bundle-path ${{ env.ATTESTATIONS_FOLDER }}/attest-build-provenance-attestation.jsonl
      - name: Upload Provenance Sigstore Bundle
        id: upload-provenance
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        with:
          name: blob-build-provenance-attestation-low-perms
          path: ${{ env.ATTESTATIONS_FOLDER }}/attest-build-provenance-attestation.jsonl
          if-no-files-found: error

  attest-metadata-low-perms:
    runs-on: ${{ inputs.workflow-runner-label }}
    needs: build-low-perms
    permissions:
      id-token: write
      attestations: write
      contents: read
      actions: read
    env:
      ARTIFACT_ID: ${{ needs.build-low-perms.outputs.blob-artifact-id }}
    outputs:
      metadata-artifact-id: ${{ steps.upload-metadata.outputs.artifact-id }}
    steps:
      - name: Fail if Runner is self-hosted
        if: ${{ runner.environment != 'github-hosted' }}
        run: |
          echo "Job is running on a self-hosted runner. Terminating job..."
          exit 1
      - name: Download Blob Artifact(s)
        if: ${{ runner.environment == 'github-hosted' }}
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6.0.0
        with:
          github-token: ${{ inputs.github-token || github.token }}
          artifact-ids: ${{ env.ARTIFACT_ID }}
          path: ${{ env.ARTIFACTS_FOLDER }}
          merge-multiple: true
      - uses: octo-sts/action@e480437973a6f6ac2e9caa40ecabedc870d76395 # v1.0.1
        id: octo-sts
        with:
          scope: liatrio
          identity: autogov-infra
      - name: Install autogov-helper
        run: |
          set +x
          export GH_TOKEN=${{ steps.octo-sts.outputs.token }}
          gh release download ${{ inputs.autogov-helper-version }} \
            --repo liatrio/autogov-helper \
            --pattern "autogov-helper"
          chmod +x autogov-helper
      - name: Generate Metadata Attestation
        env:
          GITHUB_WORKFLOW_INPUTS: ${{ toJson(inputs) }}
        run: |
          set +x
          ./autogov-helper metadata \
            --output metadata.json \
            --type blob \
            --subject-path ${{ env.ARTIFACTS_FOLDER }}
          cat metadata.json
      - name: Attest Metadata
        uses: actions/attest@daf44fb950173508f38bd2406030372c1d1162b1 # v3.0.0
        id: attest-metadata
        with:
          subject-path: ${{ env.ARTIFACTS_FOLDER }}
          predicate-type: 'https://cosign.sigstore.dev/attestation/v1'
          predicate-path: metadata.json
          push-to-registry: false
      - name: Rename Metadata Bundle
        run: |
          mkdir ${{ env.ATTESTATIONS_FOLDER }}
          mv ${{ steps.attest-metadata.outputs.bundle-path }} ${{ env.ATTESTATIONS_FOLDER }}/attest-metadata-attestation.jsonl
      - name: Emit Metadata Attestation Content
        uses: liatrio/run-with-telemetry@17cf80578d78573a8895cdbc9a4c2a1fbb707788 # latest
        with:
          otel-exporter-otlp-endpoint: ${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
          otel-service-name: ${{ env.OTEL_SERVICE_NAME }}
          github-token: ${{ inputs.github-token || github.token }}
          step-name: Emit Metadata Attestation Content
          run: |
            set +x
            export OTEL_EXPORTER_OTLP_ENDPOINT=https://${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
            ./autogov-helper att-span --bundle-path ${{ env.ATTESTATIONS_FOLDER }}/attest-metadata-attestation.jsonl
      - name: Upload Metadata Sigstore Bundle
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        id: upload-metadata
        with:
          name: blob-metadata-attestation-low-perms
          path: ${{ env.ATTESTATIONS_FOLDER }}/attest-metadata-attestation.jsonl
          if-no-files-found: error

  attest-sbom-low-perms:
    runs-on: ${{ inputs.workflow-runner-label }}
    needs: build-low-perms
    permissions:
      id-token: write
      attestations: write
      contents: read
      actions: read
    env:
      ARTIFACT_ID: ${{ needs.build-low-perms.outputs.blob-artifact-id }}
    outputs:
      sbom-artifact-id: ${{ steps.upload-sbom.outputs.artifact-id }}
    steps:
      - name: Fail if Runner is self-hosted
        if: ${{ runner.environment != 'github-hosted' }}
        run: |
          echo "Job is running on a self-hosted runner. Terminating job..."
          exit 1
      - name: Download Blob Artifact(s)
        if: ${{ runner.environment == 'github-hosted' }}
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6.0.0
        with:
          github-token: ${{ inputs.github-token || github.token }}
          artifact-ids: ${{ env.ARTIFACT_ID }}
          path: ${{ env.ARTIFACTS_FOLDER }}
          merge-multiple: true
      - uses: octo-sts/action@e480437973a6f6ac2e9caa40ecabedc870d76395 # v1.0.1
        id: octo-sts
        with:
          scope: liatrio
          identity: autogov-infra
      - name: Install autogov-helper
        run: |
          set +x
          export GH_TOKEN=${{ steps.octo-sts.outputs.token }}
          gh release download ${{ inputs.autogov-helper-version }} \
            --repo liatrio/autogov-helper \
            --pattern "autogov-helper"
          chmod +x autogov-helper
      - name: Generate SBOM
        uses: anchore/sbom-action@f8bdd1d8ac5e901a77a92f111440fdb1b593736b # v0.20.6
        with:
          path: ${{ env.ARTIFACTS_FOLDER }}
          format: cyclonedx-json
          output-file: sbom.cyclonedx.json
          upload-release-assets: false
          upload-artifact: false
          github-token: ${{ inputs.github-token || github.token }}
      - name: Log SBOM File
        run: cat sbom.cyclonedx.json | jq -r
      - name: Generate SBOM attestation
        id: attest-sbom
        uses: actions/attest-sbom@4651f806c01d8637787e274ac3bdf724ef169f34 # v3.0.0
        with:
          subject-path: ${{ env.ARTIFACTS_FOLDER }}
          sbom-path: sbom.cyclonedx.json
          github-token: ${{ inputs.github-token || github.token }}
          push-to-registry: false
      - name: Rename SBOM Bundle
        run: |
          mkdir ${{ env.ATTESTATIONS_FOLDER }}
          mv ${{ steps.attest-sbom.outputs.bundle-path }} ${{ env.ATTESTATIONS_FOLDER }}/attest-sbom-attestation.jsonl
      - name: Emit SBOM Attestation Content
        uses: liatrio/run-with-telemetry@17cf80578d78573a8895cdbc9a4c2a1fbb707788 # latest
        with:
          otel-exporter-otlp-endpoint: ${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
          otel-service-name: ${{ env.OTEL_SERVICE_NAME }}
          github-token: ${{ inputs.github-token || github.token }}
          step-name: Emit SBOM Attestation Content
          run: |
            set +x
            export OTEL_EXPORTER_OTLP_ENDPOINT=https://${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
            ./autogov-helper att-span --bundle-path ${{ env.ATTESTATIONS_FOLDER }}/attest-sbom-attestation.jsonl
      - name: Upload SBOM Attestation
        id: upload-sbom
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        with:
          name: blob-sbom-attestation-low-perms
          path: ${{ env.ATTESTATIONS_FOLDER }}/attest-sbom-attestation.jsonl
          if-no-files-found: error

  attest-dependency-scan-low-perms:
    runs-on: ${{ inputs.workflow-runner-label }}
    needs: build-low-perms
    permissions:
      id-token: write
      attestations: write
      contents: read
      actions: read
    env:
      ARTIFACT_ID: ${{ needs.build-low-perms.outputs.blob-artifact-id }}
    outputs:
      dependency-scan-artifact-id: ${{ steps.upload-dependency-scan.outputs.artifact-id }}
    steps:
      - name: Fail if Runner is self-hosted
        if: ${{ runner.environment != 'github-hosted' }}
        run: |
          echo "Job is running on a self-hosted runner. Terminating job..."
          exit 1
      - name: Download Blob Artifact(s)
        if: ${{ runner.environment == 'github-hosted' }}
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6.0.0
        with:
          github-token: ${{ inputs.github-token || github.token }}
          artifact-ids: ${{ env.ARTIFACT_ID }}
          path: ${{ env.ARTIFACTS_FOLDER }}
          merge-multiple: true
      - name: Scan current project
        uses: anchore/scan-action@f6601287cdb1efc985d6b765bbf99cb4c0ac29d8 # v7.0.0
        id: anchore-scan
        with:
          path: ${{ env.ARTIFACTS_FOLDER }}
          output-format: json
          output-file: results.json
          fail-build: false
      - uses: octo-sts/action@e480437973a6f6ac2e9caa40ecabedc870d76395 # v1.0.1
        id: octo-sts
        with:
          scope: liatrio
          identity: autogov-infra
      - name: Install autogov-helper
        run: |
          set +x
          export GH_TOKEN=${{ steps.octo-sts.outputs.token }}
          gh release download ${{ inputs.autogov-helper-version }} \
            --repo liatrio/autogov-helper \
            --pattern "autogov-helper"
          chmod +x autogov-helper
      - name: Generate Dependency Scan Attestation
        run: |
          set +x
          ./autogov-helper depscan \
            --type blob \
            --results-path results.json \
            --subject-path ${{ env.ARTIFACTS_FOLDER }} \
            --output depscan.json
          cat depscan.json
      - name: Attest Dependency Scan
        uses: actions/attest@daf44fb950173508f38bd2406030372c1d1162b1 # v3.0.0
        id: attest-dep-scan
        with:
          predicate-type: 'https://in-toto.io/attestation/vulns/v0.2'
          predicate-path: depscan.json
          push-to-registry: false
          subject-path: ${{ env.ARTIFACTS_FOLDER }}
      - name: Rename Dependency Scan Bundle
        run: |
          mkdir ${{ env.ATTESTATIONS_FOLDER }}
          mv ${{ steps.attest-dep-scan.outputs.bundle-path }} ${{ env.ATTESTATIONS_FOLDER }}/attest-dep-scan-attestation.jsonl
      - name: Emit Dependency Scan Attestation Content
        uses: liatrio/run-with-telemetry@17cf80578d78573a8895cdbc9a4c2a1fbb707788 # latest
        with:
          otel-exporter-otlp-endpoint: ${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
          otel-service-name: ${{ env.OTEL_SERVICE_NAME }}
          github-token: ${{ inputs.github-token || github.token }}
          step-name: Emit Dependency Scan Attestation Content
          run: |
            set +x
            export OTEL_EXPORTER_OTLP_ENDPOINT=https://${{ env.OTEL_EXPORTER_OTLP_ENDPOINT }}
            ./autogov-helper att-span --bundle-path ${{ env.ATTESTATIONS_FOLDER }}/attest-dep-scan-attestation.jsonl
      - name: Upload Dependency Scan Sigstore Bundle
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        id: upload-dependency-scan
        with:
          name: blob-dep-scan-attestation-low-perms
          path: ${{ env.ATTESTATIONS_FOLDER }}/attest-dep-scan-attestation.jsonl
          if-no-files-found: error
